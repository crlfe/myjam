{
  "private": true,
  "license": "AGPL-3.0-or-later",
  "author": "Chris Wolfe (https://crlfe.ca/)",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "myjam-eslint",
    "packages:build": "pnpm -r --filter './packages/**' run build",
    "packages:watch": "pnpm -r --filter './packages/**' --parallel run watch"
  },
  "packageManager": "pnpm@10.16.0",
  "minimumReleaseAge": 10080,
  "devDependencies": {
    "myjam-config": "workspace:",
    "prettier": "^3.7.4"
  }
}
